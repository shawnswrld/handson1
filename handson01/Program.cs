using System;

// Set the namespace to be HandsOn - note the capital H and O
namespace handson01
{
    class Program
    {
        static void Main(string[] args)
        {
            // extra { not needed
            //{
                int x = 3;
                int y = 4;
                int sum = 7;

                // Define an integer variable named sum that is initialized using an expression that produce the sum of variables x and y.
                // So not a bool type, but an int type here.  Don't forget your semi-colons
                bool sum = true
 

        // extra } not needed
        //}

            // extra { not needed
            //{ 
            
            // text1 and text2 should be a string type.  Strings must be surrounded with the double quotes "
            int text1 = Hello;
            int text2 = World;

            // the message variable should be defined, it was only initialized and the type is unknown
            message = ("text1" + "text2")
            return message
            
            // Any code after a return statement (line 33) will never run.  But sure to have all code appear before the return statement.
            // The output should be the concatentation of text1 and text2, you have hard coded "hello world" below.
            Console.WriteLine("Hello World!");
        }
    }
}

// Things that are missing:
// 
// In the program Main entry point method, add a single-line comment that contains the text: 
// "Main method demonstrates the use of variables, operators, expressions, and statements".
//
// 